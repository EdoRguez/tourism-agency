export interface TourismEvent {
    type: string | null,
    numberPeople: number | null,
    ticket: string | null,
    logding: string | null,
    initDate: Date | null,
    endDate: Date | null,
    fullday: string | null,
    night: string | null,
}